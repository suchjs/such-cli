module.exports = {
  OPTION_ROOT_DIRECOTRY: ["-r, --root <directory>",
  "Set the root directory, the parent directory of the such.config.js file, other directories in config will base on it."],
  ALLOW_METHODS: ['get', 'post', 'put', 'delete']
};