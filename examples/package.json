{
  "name": "such-example",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "mock": "npx such serve -r . -d -i",
    "serve": "webpack serve --mode=development",
    "add": "npx such template add -r . --",
    "show": "npx such template show -r . --",
    "rm": "npx such template rm -r . --"
  },
  "license": "MIT",
  "devDependencies": {
    "html-webpack-plugin": "^5.5.0",
    "such-cli": "^0.3.5",
    "webpack": "^5.76.0",
    "webpack-cli": "^4.9.2",
    "webpack-dev-server": "^4.7.4"
  },
  "dependencies": {
    "axios": "^0.26.0"
  }
}
